# 天合小队 Sky Team —— 设计文档

> 这是项目的**完整说明书**：机制来源、双模式区别、全部游戏规则、与原版差异、踩坑历史、待办方向。
> 一份在手，回看 6 个月后也能秒懂整个项目。

---

## 1. 项目目标

**Sky Team Web 联机版**：Luc Rémond 的合作桌游 **Sky Team（天作之合，2024 年 Spiel des Jahres 大奖）** 的浏览器实现，主题「天合小队」——机长与副驾两位拍档合作降落。

### 为什么做这个
- 个人开发者，单人开发友好
- 核心机制（秘密骰子 + 受限通信 + 双人合作）非常适合数字化
- 零依赖 HTML + Node 服务端，URL 分享即可双人联机

---

## 2. 机制来源：Sky Team（天作之合）

### Sky Team 一句话
两人合作驾驶客机在 7 轮内安全降落。每轮掷 4 颗骰子秘密分配到驾驶舱槽位，禁谈点数，靠"放置位置"和"时机"沟通。

### 玩家
- **机长（Pilot）**：蓝骰，负责姿态/引擎/起落架/刹车
- **副驾（Co-pilot）**：橙骰，负责姿态/引擎/襟翼
- **强制双人**，没有 AI/Solo 模式（原版实体桌游）

### 单局时长
约 15-20 分钟（含讨论），短平快。

### 核心张力
掷完骰子之后**禁止交流点数**，但你们要一起把飞机安全落地——只能靠：
- 讨论阶段（掷骰前）约定策略
- 放置顺序本身传递信息（先放什么槽位暗示意图）
- 对方放置的位置 + 自己的骰子值推理

---

## 3. 两种游戏模式（**重要：必读**）

打开 `http://127.0.0.1:8080/` 后根据 URL 参数决定模式：

| 模式 | 触发条件 | 用途 | 是否守游戏规则 |
|---|---|---|---|
| **联机模式** | URL 带 `?room=&role=pilot\|copilot\|passenger` | **正式玩法**（推荐） | ✅ **严格守规则**（服务端仲裁） |
| 本地模式 | URL 无参数 | **单人演示/学习规则/快速看双方信息** | ✅ **守规则**（v0.3.4 起，与联机一致） |

### 3.1 联机模式（**核心玩法**）

```
node server.js   # 启动服务端（默认 8080）

# 机长打开：
http://localhost:8080/?room=sky&role=pilot

# 副驾在另一台设备/另一标签页打开：
http://localhost:8080/?room=sky&role=copilot

# 想看双方上帝视角？再加一个标签：
http://localhost:8080/?room=sky&role=passenger
```

**特点**：
- 服务端（`server.js`）持有唯一权威状态，所有规则在服务端执行
- 每个客户端**只看到自己**的骰子和自己放的槽位（对方骰子数值**服务端过滤**隐藏）
- 对方的"放置位置"和"操作"实时可见（通过 WebSocket 推送）
- 断线自动重连（3 秒间隔）
- 顶栏常驻显示"我是机长/副驾/乘客"+ 对方在线状态
- 操作者提示：轮到谁，谁的面板**发光高亮**

**两人测试的最快方法**：同一浏览器开两个标签页，一个 `?role=pilot` 一个 `?role=copilot`，背对背假装对方（也可以并排讨论）。

### 3.2 本地模式

URL 无参数打开（默认 `start` 屏幕）。

**v0.3.4 起的本地模式**：
- 严格遵守所有游戏规则（与你扮演的角色绑定，不能连放对方）
- 顶栏可切换身份（点"机长/副驾/乘客"按钮）
- 单人玩时**每轮需要切两次身份**（机长放 → 切副驾放 → 切机长看下一轮）

**单人快速测试推荐**：用联机模式开两个标签页，更顺。

### 3.3 绝对不要做的事
- ❌ **本地模式下指望"我一个人就能代操双方"**——这是 Sky Team 的**双人合作本质**，单人玩就是要切身份
- ❌ **联机模式用 `file://` 直接打开**——服务端连不上，必须经过 `node server.js`
- ❌ **不重启 server 就改 `server.js`**——重启后房间状态清空

---

## 4. 完整游戏规则

### 4.1 棋子
- 每方 4 颗 d6 骰子（蓝/橙）
- 进近轨道：6 格（距离 6 → 0），终点是机场
- 高度表：7 → 0（每轮结束 -1）
- 起始空中交通：1 架飞机在距离 3 的位置（红色 🛩 标记）
- 重掷标记 ×1
- 咖啡标记（每轮累计，最多 3 个）

### 4.2 每轮流程（4 阶段）

#### 阶段 1：策略讨论
- **可以**公开讨论策略
- **禁止**提及骰子具体点数
- 准备好后由任一玩家点"开始掷骰"

#### 阶段 2：秘密掷骰
- 双方各自秘密掷 4 颗骰子
- 掷完后禁止交流点数（骰子值只能在揭示阶段看到）
- 任何时候可使用"重掷标记"（双方骰子全部重掷，消耗标记；标记可保留到下轮）

#### 阶段 3：放置（**严格轮流**）
- **每轮每方恰好 4 个骰子，轮流放置**（机长放 1 个 → 副驾放 1 个 → 交替），共 8 步
- 起始玩家交替（奇数轮机长先，偶数轮副驾先）
- 每个骰子只能放一次
- 槽位限制：

| 槽位 | 归属 | 强制？ | 骰值限制 | 数量 | 激活规则 |
|---|---|---|---|---|---|
| 姿态 | 双方各 1 | **必填** | 无 | — | — |
| 引擎 | 双方各 1 | **必填** | 无 | — | — |
| 无线电 | 机长 1 / 副驾 2 | 可选 | 无（任意点数） | — | 值 = 清除第 N 格飞机 |
| 集中精力 ☕ | 双方各 1 | 可选 | 无 | — | 每放一个获得 1 咖啡标记（池上限 3） |
| 起落架 | 机长 | 可选 | **1/2、2/3、3/4、4/5** | 4 | 任意顺序激活，蓝标记 +1 |
| 襟翼 | 副驾 | 可选 | **1/2、2/3、4/5、5/6** | 4 | **须按顺序**激活，橙标记 +1 |
| 刹车 | 机长 | 可选 | **2、4、6**（须按顺序） | 3 | 按 2→4→6 顺序，刹车值 +2 |

#### 阶段 3：放置（多数效果即时生效）
- 轮流放置，每次 1 颗
- **放置当下立即生效**：无线电清障、起落架/襟翼/刹车拨杆、咖啡 +1
- **第二颗放上时立即结算**：姿态比较倾斜、引擎推进/撞机/等待模式
- 顺序是核心策略（先无线电还是先引擎）

#### 阶段 4：揭示与轮末
- 8 颗放完后可用咖啡修正（±1，每骰一次）
- 轮末：高度 -1；若高度归零未到机场 → 立即坠毁
- **着陆轮**（distance=0 且 altitude≤1）：引擎与刹车比较，不再与蓝橙标记比较
- **终局检查**（高度=0 且 distance=0，或第 7 轮结束）：起落架/襟翼/姿态/无飞机/速度

### 4.3 数值参数
```
CONFIG = {
  ROUNDS: 7,
  DICE_PER_PLAYER: 4,
  ALTITUDE_START: 7,
  DISTANCE_START: 6,
  AXIS_LIMIT: 3,
  TRAFFIC_START: [3],
  BLUE_START: 5, BLUE_MAX: 9,       // 起落架每档 +1
  ORANGE_START: 9, ORANGE_MAX: 13,   // 襟翼每档 +1（与蓝标记独立）
  BRAKE_BASE: 2, BRAKE_STEP: 2,      // 刹车值 = 2 + 2×激活数
  COFFEE_MAX: 3,
  REROLL_START: 0,
  ALTITUDE_REROLL_SPACES: [7, 4],
  APPROACH_AXIS: {},
  GEAR_COUNT: 4, FLAP_COUNT: 4, BRAKE_COUNT: 3
}
```

### 4.4 失败条件（即时）
- 姿态盘到 ±3（尾旋）
- 引擎推进时撞到飞机（碰撞）
- 引擎推进越过机场（冲出）
- 等待模式下引擎推进
- 第 7 轮引擎和 ≥ 刹车值（速度过快）

### 4.5 胜利条件
第 7 轮结束时所有终局检查通过。

---

## 5. 当前实现与原版 Sky Team 的差异

| 项 | 原版 | 本项目 | 影响 |
|---|---|---|---|
| 场景数 | 21 个 | 1 个训练场 + 占位 | 重玩性弱 |
| 特殊能力 | 有 | 无 | — |
| 模块扩展 | 湍流/风/燃料等 | 部分占位 | — |
| 玩家数 | 严格 2 人 | 2 人联机 + 观战 | — |
| 咖啡修正时机 | 放置任意骰时 | 放置任意骰时 | — |
| 高度轨重掷 | 经过标记获得 | 轮初收入 + 轮内可用 | — |
| 进近转弯 | 部分场景 | 场景 APPROACH_AXIS 配置 | 训练场无约束 |

**槽位点数/顺序、无线电配置、即时生效顺序、蓝橙独立标记、着陆轮判定已对齐官方规则书（见 `docs/SkyTeam.zh.md`）。**

**所有数值都在 `game-logic` 脚本顶部的 `CONFIG` 集中管理**——调整难度改这里即可。

---

## 6. 架构设计

### 6.1 单文件优先
- `index.html`：**全部**前端代码（CSS + 2 个 `<script>`）
  - `<script id="game-logic">`：纯逻辑层（不碰 DOM，可被 node 测试）
  - `<script id="app">`：UI 层（渲染 + 事件）
- `server.js`：**零依赖**手写 WebSocket（RFC 6455，约 100 行）
- `test.js`：逻辑层冒烟测试（node 跑 2000 局）
- `test-online.js`：联机冒烟测试（手写 WS 客户端模拟三连接）
- `DESIGN.md`：本文件

### 6.2 服务端单点逻辑
服务端启动时**从 `index.html` 提取 `<script id="game-logic">` 内容**，用 `new Function` 执行拿到 `GameLogic` 对象。客户端也跑同一份 logic。**不存在规则不同步问题**。

### 6.3 保密设计（重要）
- **服务端按角色 sanitize state 后下发**——不是靠客户端自律
- 机长收到的 state 中，副驾骰子数值 `v` 被置为 `0`，槽位数值也被清零（保留"已放置"标记）
- 揭示阶段（`phase === 'reveal'`）后 sanitize 不再过滤，所有数值公开
- 乘客（passenger）始终收全量（上帝视角）
- 即使用户打开 DevTools 也拿不到对方的隐藏信息

### 6.4 渲染层架构
- `renderAll()`：根据 `ui.mode`（local/online）和 `ui.screen`（start/game/waiting）分发
- `renderPanel(role)`：左右面板（机长/副驾）
- `renderCenter()`：中央仪表（姿态/引擎/进近/高度/揭示/终局/日志）
- 每个 `render*` 函数都有独立 `try-catch` + `state` 守卫，**任何错误都在该区域显示**，不再静默死屏

---

## 7. 已踩过的坑（防止再犯）

### 7.1 `id="id-switch"` 缺失（v0.3 → v0.3.1）
HTML 写了 `<div class="id-switch">` 没写 `id`，JS `getElementById('id-switch')` 返回 `null`，`null.style.display = ...` 抛错，整个渲染链中断。**教训**：用 `getElementById` 前一定确认 HTML 有 `id`（`class` ≠ `id`）。

### 7.2 `Map.forEach` 回调参数（v0.3 → v0.3.1）
`Map.forEach((value, key, map) => ...)`，回调**第一个参数是 value 不是 key**。我把 `Map<conn, true>` 遍历时写 `forEach(conn => ...)`，拿到的是 `true`（value）而不是连接对象（key），导致 `send(true, msg)` 静默失败。**教训**：遍历 Map 时要么用 `forEach((v, k) => ...)`，要么用 `for (const k of map.keys())`。

### 7.3 `el.classList.add('highlight co')`（v0.3.2 → v0.3.3）
classList 单参数含空格 → `DOMException: The string contains invalid characters`。**教训**：`classList.add` 多 token 必须用多参数 `add('a', 'b')`，或 split + forEach。

### 7.4 本地模式违反轮流规则（v0.3.4 修复）
为了让单人测试更方便，我曾把本地模式改成"点谁代操谁"（自动切 currentPlayer）。这**打破了"每轮每方 4 颗骰子必须轮流放置"的规则**——机长能连放 4 个。修复：恢复严格轮流，viewer 锁定角色，单人玩需切身份。

### 7.5 服务端 `room.id` 缺失（v0.3）
`getRoom` 创建 room 时漏写 `id: id` 字段，功能无碍但日志混乱。修复：补上。

### 7.6 浏览器缓存顽固
Chrome 对同一 URL 强刷（Ctrl/Cmd+Shift+R）有时仍走 disk cache。**最稳妥**：用**无痕窗口**（Ctrl/Cmd+Shift+N），或加查询参数（`?v=N`）bust 缓存。

---

## 8. 调试设施（v0.3 加）

页面右下角**永显调试角标**：显示当前 `mode / screen / role / round / phase / altitude / distance`。
任何 JS 异常**直接红色错误块显示在页面上**（含堆栈）。
**绝不再完全空白**——下次出问题你能直接看到原因。

---

## 9. 待办与扩展方向

### 短期
- [ ] 真实手感试玩（机长副驾各开一页，跑 1-2 局验证张力）
- [ ] 数值平衡：根据试玩调整 `CONFIG`（起始飞机位置、姿态极限、刹车判定等）
- [ ] 移动端触屏适配（玩家反馈说想手机玩）

### 中期
- [ ] 多机场场景（原版 21 个）：每个机场一个 module 槽位/参数
- [ ] 湍流/风/燃料模块（参考原版后续扩张）
- [ ] 特殊能力（机场奖励）

### 长期
- [ ] 在线大厅（多房间列表、观战列表）
- [ ] 简单文字聊天（讨论阶段辅助）
- [ ] PWA / 离线支持
- [ ] 移动 App 壳（Capacitor 包装）

---

## 10. 运行 / 测试命令速查

```bash
# 启动联机服务端
cd sky-squad
node server.js                          # 默认 8080，PORT=xxx 可改

# 跑逻辑回归测试（2000 局启发式模拟）
node test.js 2000

# 跑联机冒烟测试（手写 WS 客户端，需要 server 在跑）
node test-online.js

# 浏览器打开（本地模式）
http://localhost:8080/

# 浏览器打开（联机模式）
http://localhost:8080/?room=sky&role=pilot
http://localhost:8080/?room=sky&role=copilot
http://localhost:8080/?room=sky&role=passenger   # 观战
```

---

## 11. 修改约定

- **规则参数**改 `index.html` 中 `<script id="game-logic">` 顶部的 `CONFIG`（前后端共用）
- **新增场景**改 `TRAFFIC_START`（数组，每元素一架起始飞机位置）
- **新增槽位**改 `SLOTS`（机长/副驾各自的槽位定义）
- **新增结算规则**改 `resolveRound` 函数
- **UI 调整**改 `<script id="app">` 内的 `render*` 函数
- **每次改 game-logic 后**跑 `node test.js 1000` 确认无回归
- **每次改 server.js 后**重启 server（`pkill -f "node server.js"` 后再 `node server.js &`）